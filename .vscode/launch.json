{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (CodeLLDB)",
            "type": "lldb", // ★★★ "cppdbg" から "lldb" に変更 ★★★
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: g++-15 build active file"
        }
    ]
}
